<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>마이페이지</title>
    <!-- <link rel="stylesheet" href="/css/bootstrap.min.css"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
</head>

<body class="bg-dark">
    <%- include('nav.ejs', { user : user }) %>
    <div class="container mt-4">
        <h2 class="text-white">마이페이지</h2>
        <hr class="hr-white-bold">
        <div class="row mb-3">
            <div class="col-md-6">
                <% if (user.img) { %>
                    <img src="<%= user.img %>" class="d-block mx-auto profile-img">
                <% } else { %>
                    <img src="https://taesungchoiforum1.s3.ap-northeast-2.amazonaws.com/man_pictogram.jpg" class="d-block mx-auto profile-img">
                <% }%>
                
            </div>
            <div class="col-md-6">
                <div>
                    <label for="name" class="form-label text-white">이름</label>
                    <p type="text" class="form-control" id="name" name="name"><%= user.name %></p>
                </div>
                <div>
                    <label for="age" class="form-label text-white">나이</label>
                    <p type="text" class="form-control" id="age" name="age"><%= user.age %></p>
                </div>
            </div>
        </div>
        <div>
            <label for="texts" class="form-label text-white">소개글</label>
            <p class="form-control">
                <%= user.texts %>
            </p>
        </div>
        <a href="/mypage_edit/<%= user._id %>" class="btn btn-outline-light">수정</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        
    </script>
</body>

</html>